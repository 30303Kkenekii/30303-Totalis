package org.firstinspires.ftc.teamcode;

import com.pedropathing.geometry.Pose;

public class PoseStorage {
    // Default fallback in case Auto isn't run
    public static Pose currentPose = new Pose(8, 8, 0);
}